# Copy to .env.local for a permanent local config (optional).
# Defaults are http://localhost:8080 and ws on same host if not set.
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=http://localhost:8080/ws
